/*
 Minimal placeholder build.gradle to satisfy CI presence checks.
 This file is not intended to build a native app in this environment.
*/
buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        // Intentionally empty for CI placeholder
    }
}
allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
task clean(type: Delete) {
    delete rootProject.buildDir
}
